[build-system]
requires = [
    "setuptools>=46.4.0",
    "wheel",
]
build-backend = "setuptools.build_meta"

[tool.coverage.run]
source = [
    "penaltymodel/",
    ]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "RuntimeError",
    "pass",
]
